<?php
require_once(APPLICATION_PATH.'/jqgrid/action/action_linkage.php');

class property_property_action_linkage extends action_linkage{//}action_jqgrid{
	// public function calcSqlComponents($params, $limited = true){
		// print_r($params);
		// $ret = parent::calcSqlComponents($params, $limited);
		// // $ret['main']['fields'] = "DISTINCT property.id, property.name";
		// // $ret['main']['from'] .= " left join device_type_property on device_type_property.property_id=property.id";
		// // $ret['where'] = " device_type_property.device_type_id=".$params['device_type_id'];
// print_r($ret);		
		// return $ret;
	// }
	// protected function handlePost(){
// print_r($this->params);
		// $sql = "select device_type_property.*, property.name from device_type_property left join property on device_type_property.property_id=property.id WHERE device_type_id={$this->params['device_type_id']}";
		// $res = $this->
		// $ret = parent::handlePost();
		// return $ret['rows'];
	// }
}

?>